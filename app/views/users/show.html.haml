#user-profile
  .row
    .span3
      = render 'avatar', user: @user, size: "large", kind: nil, id: 'avatar-preview'
    .span8
      %h1= @user.name
      %p.username= "@" + @user.username
      %p.email= mail_to @user.email

      -if @groups
        %h3#user-groups-list= t(:groups)
        %ul
        - @groups.each do |group|
          %li
            = link_to group.name, group_path(group)
